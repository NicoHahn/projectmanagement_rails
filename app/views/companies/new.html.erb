<h1>Create Company</h1>
<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name  %>
  <%= f.hidden_field :creator_id, value: current_user.id %>
  <%= f.submit "Company erstellen"%>
<%end%>
